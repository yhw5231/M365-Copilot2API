# Place a file named `m365_admin_password` here (mode 0o600) to bootstrap the
# admin password via the M365_ADMIN_PASSWORD_BOOTSTRAP_FILE secret. This
# directory is bind-mounted into /run/secrets and read-only. The pre-existing
# empty directory ensures the mount point exists before the first deploy.